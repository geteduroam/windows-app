<Window
    x:Class="App.Library.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:App.Library.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:App.Library"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:templates="clr-namespace:App.Library.Templates"
    xmlns:lang = "clr-namespace:EduRoam.Localization;assembly=EduRoam.Localization"
    Title="{Binding AppTitle}"
    Width="420"
    Height="600"
    MinWidth="420"
    MinHeight="420"
    MaxWidth="420"
    Closing="MainWindow_OnClosing"
    mc:Ignorable="d"
    xmlns:b = "clr-namespace:App.Library.Binding">
    <!--<WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="8" />
    </WindowChrome.WindowChrome>-->
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Templates/_Index.xaml" />
                <ResourceDictionary>
                    <templates:LocalDataTemplateSelector x:Key="LocalDataTemplateSelector" />
                    <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
                    <converters:ImageConverter x:Key="ImageConverter" />
                    <converters:ImageConverter x:Key="AppImageConverter" AppSpecific="True" />
                    <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
                    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
                    <b:MainViewModelProxy x:Key="proxy" DataCtx="{Binding}" />
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Background="{DynamicResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Height="50">
            <Image Source="{Binding Converter={StaticResource AppImageConverter}, ConverterParameter=logo.png}" Stretch="Uniform" />
        </Grid>

        <Grid
            Grid.Row="1"
            Grid.RowSpan="2"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="{DynamicResource BackgroundBrush}"
            Opacity="0.5"
            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
            ZIndex="100">
            <TextBlock
                Margin="5"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="24"
                Text="{x:Static lang:Resources.Loading}"
                TextAlignment="Center" />
        </Grid>

        <Grid
            Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"
            Visibility="{Binding ActiveContent, Converter={StaticResource NullToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Button
                Grid.Row="1"
                Width="186"
                Height="56"
                Margin="0,0,0,32"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Command="{Binding NewProfileCommand}"
                Style="{DynamicResource BlueButtonStyle}">
                <TextBlock
                    x:Name="tbNewProfile"
                    Text="{x:Static lang:Resources.ConnectToEduroam}"
                    TextAlignment="Center"
                    TextWrapping="Wrap" />
            </Button>
        </Grid>

        <ContentControl
            Grid.Row="1"
            Content="{Binding ActiveContent}"
            ContentTemplateSelector="{DynamicResource LocalDataTemplateSelector}" />

        <Grid Grid.Row="2" 
              Visibility="{Binding ActiveContent, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=inverse}" 
              Margin="5, 5, 5, 5">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="20" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="32"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="32"/>
            </Grid.ColumnDefinitions>
            
            <!--<b:BindingProxy x:Key="proxy" Data="{Binding}" />-->
            <Button Command="{Binding OpenMenuCommand}"
                    Visibility="{Binding ActiveContent.ShowMenu, Converter={StaticResource BooleanToVisibilityConverter}}"
                    Width="32" Height="32" Background="Transparent" BorderBrush="Transparent" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="24" 
                    Grid.Row="0"
                    Grid.Column="0">
                <Image Width="24" Height="24" 
                       Source="{Binding Converter={StaticResource ImageConverter}, ConverterParameter=Images/menu-icon.png}" Stretch="Uniform" />
                <Button.ContextMenu>
                    <ContextMenu IsOpen="{Binding DataCtx.ShowMenu, Source={StaticResource proxy}}" >
                        <MenuItem Header="{x:Static lang:Resources.MenuItemLoadEAP}" 
                                  Command="{Binding DataCtx.LoadEapFileCommand, Source={StaticResource proxy}}"
                                  IsEnabled="{Binding DataCtx.CanEapFileBeLoaded, Source={StaticResource proxy}}"/>
                        <Separator/>
                        <MenuItem Header="{x:Static lang:Resources.MenuItemRefresh}" 
                                  Command="{Binding DataCtx.RefreshCommand, Source={StaticResource proxy}}"
                                  IsEnabled="{Binding DataCtx.IsARefreshPossible, Source={StaticResource proxy}}"/>
                        <MenuItem Header="{x:Static lang:Resources.MenuItemReauthenticate}" 
                                  Command="{Binding DataCtx.ReauthenticateCommand, Source={StaticResource proxy}}"
                                  IsEnabled="{Binding DataCtx.IsReauthenticatePossible, Source={StaticResource proxy}}"/>
                        <Separator/>
                        <MenuItem Header="{x:Static lang:Resources.MenuItemRemoveProfile}" 
                                  Command="{Binding DataCtx.RemoveProfileCommand, Source={StaticResource proxy}}"
                                  IsEnabled="{Binding DataCtx.CanProfileBeRemoved, Source={StaticResource proxy}}"/>
                        <MenuItem Header="{x:Static lang:Resources.MenuItemRemoveCertificates}" 
                                  Command="{Binding DataCtx.RemoveCertificatesCommand, Source={StaticResource proxy}}"
                                  IsEnabled="{Binding DataCtx.CanCertificatesBeRemoved, Source={StaticResource proxy}}"/>
                        <MenuItem Header="{x:Static lang:Resources.MenuItemUninstall}" 
                                  Command="{Binding DataCtx.UninstallCommand, Source={StaticResource proxy}}"
                                  IsEnabled="{Binding DataCtx.CanAppBeUninstalled, Source={StaticResource proxy}}"/>
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Height="42"
                        Grid.Row="0"
                        Grid.Column="1">
                <Button
                        Width="131"
                        Height="42"
                        Margin="10,0,10,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Command="{Binding ActiveContent.PreviousCommand}"
                        Content="{x:Static lang:Resources.ButtonPrevious}"
                        Style="{DynamicResource BlueButtonStyle}"
                        Visibility="{Binding ShowNavigatePrevious, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <Button
                        Width="131"
                        Height="42"
                        Margin="10,0,10,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Command="{Binding ActiveContent.NextCommand}"
                        Content="{x:Static lang:Resources.ButtonNext}"
                        IsDefault="true"
                        Style="{DynamicResource BlueButtonStyle}" 
                        Visibility="{Binding ShowNavigateNext, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>

            <Button Command="{Binding OpenHelpCommand}"
                    Visibility="{Binding ActiveContent.ShowHelp, Converter={StaticResource BooleanToVisibilityConverter}}"
                    Width="32" Height="32" Background="Transparent" BorderBrush="Transparent" 
                    Grid.Row="0"
                    Grid.Column="2">
                <Image Width="24" Height="22" Grid.Row="1" 
                       Source="{Binding Converter={StaticResource ImageConverter}, ConverterParameter=Images/help-icon.png}" Stretch="Uniform" />
            </Button>

            <Label Grid.Row="1" 
                   Grid.Column="1" 
                   Grid.ColumnSpan="2"
                   Content="{Binding AppVersion}" 
                   HorizontalAlignment="Right" 
                   VerticalAlignment="Bottom" 
                   Foreground="{DynamicResource {x:Static SystemColors.InactiveCaptionBrushKey}}" 
                   FontFamily="Tahoma" 
                   FontSize="8"/>
        </Grid>
    </Grid>
</Window>

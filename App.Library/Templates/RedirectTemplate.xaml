<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewModels="clr-namespace:App.Library.ViewModels"
    xmlns:lang = "clr-namespace:EduRoam.Localization;assembly=EduRoam.Localization">

    <DataTemplate DataType="{x:Type viewModels:RedirectViewModel}">
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                
                <TextBlock
                    x:Name="tbTitle"
                    Margin="5"
                    FontSize="24"
                    Text="{x:Static lang:Resources.RedirectTitle}"
                    TextAlignment="Center"
                    TextWrapping="Wrap" />
                
                <TextBlock
                    x:Name="tbRedirect"
                    Margin="5"
                    TextAlignment="Center"
                    TextWrapping="Wrap">
                    <Hyperlink x:Name="hlink" Command="{Binding NextCommand}">
                        <TextBlock Text="{x:Static lang:Resources.RedirectText}" />
                    </Hyperlink>
                </TextBlock>
            </StackPanel>
        </Grid>
    </DataTemplate>

</ResourceDictionary>
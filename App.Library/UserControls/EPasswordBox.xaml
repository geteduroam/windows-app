<UserControl x:Class="App.Library.UserControls.EPasswordBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:App.Library.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="800"
             x:Name="root">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="36" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <PasswordBox
                    Grid.Column="0"
                     x:Name="PasswordBox"
                     Visibility="{Binding IsPasswordHidden, ElementName=root, Converter={StaticResource BooleanToVisibilityConverter}}"
                     PasswordChanged="PasswordBox_PasswordChanged"
                     Padding="2,3,2,3" />
        
        <TextBox
                Grid.Column="0"
                 x:Name="TextBox"
                 Text="{Binding Value, ElementName=root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Visibility="{Binding IsPasswordRevealed, ElementName=root, Converter={StaticResource BooleanToVisibilityConverter}}"
                 Padding="2,3,2,3" />
        
        <Button 
                Grid.Column="1"
                Click="ToggleButton_Click"
                HorizontalAlignment="Right"
                Width="35"
                Margin="0,0,0,0">
            👁️
        </Button>

    </Grid>
</UserControl>

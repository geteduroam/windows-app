<Page x:Class="WpfApp.Menu.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Menu"
      xmlns:c="clr-namespace:WpfApp.Classes"
      mc:Ignorable="d" 
      d:DesignHeight="345" d:DesignWidth="322"
      Title="Login">

    <StackPanel Height="345" Width="322" Background="White" HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel Background="White"/>

        <TextBlock x:Name="tbTitle" TextWrapping="Wrap" Text="Login" TextAlignment="Center" Margin="5" FontSize="14"/>
        <Grid x:Name="gridCred" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="tbCredInfo" TextWrapping="Wrap" Text="Enter your username and password" TextAlignment="Center" Margin="5" Grid.ColumnSpan="3"/>

            <TextBox x:Name="tbUsername" Text="" Margin="5" Grid.ColumnSpan="1" Grid.Column="1" Grid.Row="1" TextChanged="tbUsername_TextChanged" LostFocus="tbUsername_LostFocus"/>
            <TextBlock IsHitTestVisible="False" Text="Username" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="8,0,0,0" Foreground="DarkGray" Grid.Column="1" Grid.Row="1" >
                <TextBlock.Style>
                    <Style TargetType="{x:Type TextBlock}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Text, ElementName=tbUsername}" Value="">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <TextBlock IsHitTestVisible="False" Text="Username" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="8,0,0,0" Foreground="DarkGray" Grid.Column="1" Grid.Row="1" >
                <TextBlock.Style>
                    <Style TargetType="{x:Type TextBlock}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Text, ElementName=tbUsername}" Value="">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <PasswordBox x:Name="pbCredPassword"  Margin="5" Grid.Row="2" Grid.Column="1" Grid.RowSpan="1" Height="18" Password="" PasswordChanged="pbCredPassword_PasswordChanged"/>

            <TextBlock x:Name="tbRealm" TextWrapping="Wrap" Text="" TextAlignment="Left" Margin="0,5,5,5" Grid.Column="2" Grid.RowSpan="3" Grid.Row="1"/>
            <TextBlock x:Name="tbRules" TextWrapping="Wrap" Text="" TextAlignment="Left" Margin="5" Grid.Row="3" Grid.ColumnSpan="3" HorizontalAlignment="Left"/>
        </Grid>
        <Grid x:Name="gridCert" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="tbCertInfo" Text="Enter password for the certificate" TextWrapping="Wrap" TextAlignment="Center" Margin="5" Grid.ColumnSpan="3"/>
            <PasswordBox x:Name="pbCertPassword"  Margin="5" Password="" Grid.ColumnSpan="1" Grid.Row="1" Grid.Column="1"/>
        </Grid>

        <TextBlock x:Name="tbStatus" TextWrapping="Wrap" Text="" TextAlignment="Center" Margin="5" />
    </StackPanel>
</Page>

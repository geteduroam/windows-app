<Page x:Class="WpfApp.Menu.SelectProfile"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp.Menu"
      mc:Ignorable="d" d:DesignWidth="300" d:DesignHeight="360"
      FontSize="14" SnapsToDevicePixels="True"
      Title="SelectProfile">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="tbTitle" TextWrapping="Wrap" Text="TextBlock" TextAlignment="Center" Margin="24,40,24,24" Grid.Row="0" FontSize="24"/>
        <ListBox x:Name="lbProfiles" ItemContainerStyle="{StaticResource ListBoxItemStyle}" Grid.Row="1" Margin="24" SelectionChanged="lbProfiles_SelectionChanged" MouseDoubleClick="lbProfiles_MouseDoubleClick"/>
    </Grid>
</Page>
